{
  "name": "api-tests",
  "version": "1.0.0",
  "description": "Testes automatizados para API RESTful de usuários",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/**/*.test.js --timeout 10000",
    "report": "allure generate allure-results --clean -o allure-report && allure open"
  },
  "devDependencies": {
    "allure-commandline": "^2.20.1",
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3"
  }
}
